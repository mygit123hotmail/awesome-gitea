name: lint
on:
  - push
  - pull_request

jobs:
  lint:
    name: lint
    runs-on: ubuntu-latest
    steps:
      - name: lint
        uses: https://github.com/articulate/actions-markdownlint@v1
        with:
          files: '**/*.md'